[project]
name = "markaestro"
version = "0.1.0"
description = "Client for the Markaestro public API"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.9"
dependencies = ["httpx>=0.24"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
