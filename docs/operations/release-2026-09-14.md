# September 14, 2026 release review

## Decisions included

- Four paid plans: Starter $19, Growth $29.99, Pro $59, Business $149 monthly. Annual totals: $192, $299.88, $588, $1,488. Eligible new customers receive a seven-day trial with a payment method required. Existing Stripe subscriptions and prices are preserved.
- No permanent free usage allowance. Creation and publishing enforce active paid/trial access; expired trials stop granting access. Every paid tier receives unlimited analytics history and CSV export.
- Workspace subscriptions apply to invited members and background jobs. Missing canonical records resolve through the workspace creator's legacy subscription; canonical cancellations and downgrades remain authoritative. Role permissions remain separate from plan access.
- X read accounting deduplicates resources per UTC day, settles list reservations against returned resources, and keeps the hard budget. Import walks use date cutoffs and bounded pagination. Billing-action errors are non-retryable and logged by code.
- English homepage redesign with interactive examples. Existing translated homepages remain in use until the redesigned copy is localized.

## Corrections during release review

- Draft testimonials remain in the source with `verified: false` and cannot render as customer reviews. Removed the invented aggregate rating. Example performance data is explicitly labeled.
- Removed contradictory no-card/free-plan claims, marked Evergreen as Pro/Business, and made demo action links navigate to onboarding or agent documentation.
- Replaced unsupported blanket competitor claims with a workflow comparison.
- Fixed X settlements crossing UTC midnight/month-end so adjustments affect the original reservation period. Added regression tests.
- Explicitly exclude environment files, local agent directories, and TypeScript caches from App Hosting source uploads. Runtime secrets continue to come from the configured Secret Manager bindings.

## Validation

- Full CI: lint, TypeScript, live Firestore query validation, copy rules, route contracts, capability parity, environment schema, OpenAPI, generated API docs, and unit tests.
- Production Next.js build completed; browser checks verified monthly/annual prices and French homepage content. The eight live Stripe price records match the documented amounts, intervals, products, and active/live status.
- Translation checker reports the same 51 affected namespaces as the prior commit, with no new missing keys or untranslated findings. These existing translation gaps remain follow-up work.
- Firestore providerUsageDedupe TTL deployed and verified ACTIVE. Three unrelated remote field overrides were preserved.

Application rollout and production smoke results are recorded after deployment.
